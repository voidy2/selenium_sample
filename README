[in Terminal1] --> Runninng selenium-server
$ java -jar server/selenium-server-standalone-2.3.0.jar

[in Terminal2] --> Running SBT
$ bin/sbt

[in SBT (in Terminal2)] --> Running htmlUnitDriver or firefoxDriver or InternetExploreDriver
> selenium-test
or
> selenium-test firefox
or (Windows only)
> selenium-test ie

[Output Report (junit-xml-format)]
target/reports/*.xml
